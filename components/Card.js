import styled from 'styled-components';

const CardContainer = styled.div`

margin: 0 0 0 0;
height: 20rem;
width: 100%;
background-color: purple;

`

const CardImage = styled.img`

`


const Card = ({ title, money, orgName  }) => (

    <CardContainer>

    </CardContainer>
);

export default Card;